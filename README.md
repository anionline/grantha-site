# grantha.ai

Stealth landing page.

Single static `index.html`, hosted on GitHub Pages. Custom domain configured via `CNAME` file and apex DNS records.

## Local preview

```sh
python3 -m http.server 8000
# open http://localhost:8000
```

## Deploy

Any push to `main` ships to grantha.ai.
